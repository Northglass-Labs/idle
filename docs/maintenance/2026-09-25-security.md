# Security maintenance — 2026-09-25

Updated the workspace graph, Fastify/static server dependencies, Sharp/image
parsers, Mermaid CDN and integrity metadata. The latest dependency audit has
zero high/critical entries; two moderate stream-json/Minio entries remain.
Full local typechecks and tests passed after generating Prisma. All 36 release
and container boundary tests pass on this candidate.

The relay and web image base digests were refreshed from the official registry.
The relay runtime also installs current Debian security updates before removing
package-manager tooling. A fresh container build, health smoke and Grype audit
must pass in CI; the Mac has no Docker runtime. Previous image findings must
not be described as closed until that image scan passes.

Publication remains blocked by the trusted upstream-import policy: dependency
maintenance changes package.json and existing boundary tests, which that policy
classifies as protected controls. Its policy-only bootstrap path cannot admit
these changes, and editing the gate within this PR cannot change the trusted
base workflow. No policy, environment approval or required check was bypassed.
A maintainer-approved bootstrap of a reviewed dependency-maintenance admission
path is required before this PR can merge. Existing protected publication and
private review requirements remain in force.

Install with the committed Yarn lockfile, generate Prisma, and rerun the
workspace type/test commands plus `node --test scripts/container-boundary.test.mjs
scripts/release-boundary.test.mjs`. Keep the previous deployment and verified
state backup for rollback; this candidate has not been deployed.

Metro 0.83.5/0.83.7 still passed filenames to image-size. A version- and
anchor-bound adapter now passes image bytes to the unchanged 2.0.4 parser in
every nested Metro copy, including Expo’s own resolver. Root postinstall and
the standalone web image apply it. The actual Expo path and malformed-image,
PNG/GIF/SVG, retina, ZIP-path, hash, idempotence and drift controls pass.
The fresh candidate review and 54 dependency/container/release tests pass.
The exact-head web export subsequently passed in GitHub CI.

## Final source and local verification checkpoint

At source 4e2bd9403055f4984f205c032857813018030f1f, GitHub application,
agent/CLI/server/wire tests, type checks, web release, Linux/Windows Node 22/24
smokes, container health/Grype and CodeQL checks pass. The hygiene job completes
216 tests without a failure, then fails the same protected-control policy as
static-policy. Private-publication is skipped because that prerequisite fails.

The trusted base rejects package.json and existing container/dependency boundary
tests as protected-import-control. Its policy-only bootstrap admits only the
allow/review text files; those records cannot approve protected controls. Editing
the checker in this dependency PR cannot authorize itself. A repository
maintainer must establish an independently reviewed maintenance path in the
trusted policy, then rerun the normal exact-head checks and publication review.
No force merge, workflow weakening or branch-protection change was performed.

The clean canonical local checkout was older than the repository history rewrite.
Its old branch and a hash-verified installed-dependency backup are retained. A
separate local branch now uses this tested candidate: frozen Yarn install, full
workspace typecheck and four installed Metro boundary controls pass. Audit has
zero high/critical and two moderate findings. This is local uptake only; it does
not establish a new hosted or native release. Historical/dirty Idle worktrees
were preserved.

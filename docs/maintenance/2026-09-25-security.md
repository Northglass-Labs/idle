# Security maintenance — 2026-09-25

Updated the workspace graph, Fastify/static server dependencies, Sharp/image
parsers, Mermaid CDN and integrity metadata. The latest dependency audit has
zero high/critical entries; two moderate stream-json/Minio entries remain.
Full local typechecks and tests passed after generating Prisma. All 36 release
and container boundary tests pass on this candidate.

The relay and web image base digests were refreshed from the official registry.
The relay runtime also installs current Debian security updates before removing
package-manager tooling. A fresh container build, health smoke and Grype audit
must pass in CI; the Mac has no Docker runtime. Previous image findings must
not be described as closed until that image scan passes.

Publication remains blocked by the trusted upstream-import policy: dependency
maintenance changes package.json and existing boundary tests, which that policy
classifies as protected controls. Its policy-only bootstrap path cannot admit
these changes, and editing the gate within this PR cannot change the trusted
base workflow. No policy, environment approval or required check was bypassed.
A maintainer-approved bootstrap of a reviewed dependency-maintenance admission
path is required before this PR can merge. Existing protected publication and
private review requirements remain in force.

Install with the committed Yarn lockfile, generate Prisma, and rerun the
workspace type/test commands plus `node --test scripts/container-boundary.test.mjs
scripts/release-boundary.test.mjs`. Keep the previous deployment and verified
state backup for rollback; this candidate has not been deployed.
